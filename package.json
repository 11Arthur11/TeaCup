{
  "name": "teacloud-frontend",
  "version": "0.1.10",
  "private": true,
  "type": "module",
  "description": "پنل مدیریت فارسی و RTL ابر چایی برای TeaSpeak و AudioBot",
  "scripts": {
    "generate:api": "node scripts/generate-openapi.mjs",
    "check": "tsc -p tsconfig.json --noEmit && node scripts/check-api-coverage.mjs",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev-server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
